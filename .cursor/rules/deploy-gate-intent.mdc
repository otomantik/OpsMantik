---
description: Mandatory before deploy — Intent multi-site test
alwaysApply: true
---

# DEPLOY GATE — Intent Test (MANDATORY)

**Intent is our backbone.** Before deploy you must run:

```bash
npm run smoke:intent-multi-site
```

No deploy without 2/2 site PASS. This test verifies the sync → events → calls flow (yapiozmendanismanlik.com, poyrazantika.com).

Details: `docs/OPS/DEPLOY_GATE_INTENT.md`
