## Summary
- Standardized debug logging via `debugLog`/`debugWarn` (keeps prod logs clean).
- Reduced `any` usage in dashboard UI (HunterCard, QualificationQueue, session drawers/groups).
- Updated tech debt audit doc.

## Test plan
- `npm run build`
- Smoke: login -> dashboard -> open a session drawer

## Notes
- `package.json` has an unstaged local change (not included).
- Untracked migration file is not included.

