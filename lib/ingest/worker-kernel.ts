/**
 * Barrel: ingest worker entrypoint. Implementation lives in `execute-ingest-command.ts`.
 * @see {@link executeIngest}
 */
export { executeIngest, type IngestLane } from '@/lib/ingest/execute-ingest-command';
